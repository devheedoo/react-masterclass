import styled from 'styled-components';

const Wrapper = styled.div`
  width: 100vw;
  height: 200vh;
  background: linear-gradient(135deg, lightgreen, tomato);
`;

export default function TVShows() {
  return <Wrapper></Wrapper>;
}
